# S07-S08 · Granularidad y calidad de los datos (Semana 4)

Material D2L de las sesiones 7 (lunes) y 8 (miércoles): lecciones HTML autocontenidas con candados CSS, sobre `andina_market.csv` — la filial ecuatoriana ficticia de Superstore con 11 problemas de calidad sembrados.

- `brightspace/` — 2 lecciones compactas (una por sesión, con índice de anclas) + `andina-market.csv` (subir todo junto)
- `andina_market.csv` + `clave.json` — dataset y clave exacta de problemas
- `guia_profesor.md` — clave de respuestas, timing y pasos de subida
- `generar_dataset.py` / `generar_figuras.py` / `generar_lecciones_brightspace.py` — todo se regenera con estos tres, en ese orden
